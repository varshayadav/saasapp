<h2>Sign up</h2>

<% if params[:plan] == '2' %>
  <h1>Pro Account</h1>
<% else %>
    <h1>Basic Account</h1>
<% end %>

<% if params[:plan] == '2' %>
    <%= render 'pro_form' %>
<% else %>
    <%= render 'basic_form' %>
<% end %>

<%= render "devise/shared/links" %>
